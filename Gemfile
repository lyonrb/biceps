source "http://rubygems.org"

gem "rails",
  :git => 'https://github.com/rails/rails.git',
  :branch => '3-1-stable' # master can't be used for now because of journey not being released yet

# To use debugger (ruby-debug for Ruby 1.8.7+, ruby-debug19 for Ruby 1.9.2+)
# gem 'ruby-debug'
# gem 'ruby-debug19'

group :test do
  gem "rspec"
end
